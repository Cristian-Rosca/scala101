package practice

object OperatorsPractice extends App {

  val y: Boolean = (4 + 3 * 50) < 300

  val x: Boolean = (96 / 12) >= 8

  val a: Boolean = "Hamster" < "Hippo"

  val b: Boolean = (8 % 2) == 0

  val c: Boolean = (150 / 8) < 20 && 54 < 20 && (54 / 4) < 14

}
